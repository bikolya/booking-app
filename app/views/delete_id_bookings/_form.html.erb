<%= form_for(@delete_id_booking) do |f| %>
  <% if @delete_id_booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@delete_id_booking.errors.count, "error") %> prohibited this delete_id_booking from being saved:</h2>

      <ul>
      <% @delete_id_booking.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :country_id %><br>
    <%= f.number_field :country_id %>
  </div>
  <div class="field">
    <%= f.label :flight_id %><br>
    <%= f.number_field :flight_id %>
  </div>
  <div class="field">
    <%= f.label :hotel_id %><br>
    <%= f.number_field :hotel_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
